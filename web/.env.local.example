# Local-dev only. Copy to .env.local (never commit .env.local).
# When deploying: set NEXT_PUBLIC_API_BASE_URL in Vercel project settings
# to your Render backend URL (e.g. https://pulscale-api.onrender.com).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
